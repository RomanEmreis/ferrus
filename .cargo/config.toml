# Test configuration for ferrus
# Note: There's no built-in Cargo config option to set RUST_TEST_THREADS
# Tests should be run with: cargo test -- --test-threads=1
# Or: RUST_TEST_THREADS=1 cargo test
